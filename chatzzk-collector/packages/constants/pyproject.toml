[project]
name = "chatzzk-constants"
version = "0.1.0"
description = "Global constants for the chatzzk project."
requires-python = ">=3.11"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/chatzzk_constants"]

[build-system]
requires = ["hatchling>=1.27.0"]
build-backend = "hatchling.build"
