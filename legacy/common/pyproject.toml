[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "chatzzk-common"
version = "0.1.0"
description = "Shared library for the Chatzzk project"
dependencies = [
    "minio",
    "loguru",
    "pydantic",
    "orjson",
]
